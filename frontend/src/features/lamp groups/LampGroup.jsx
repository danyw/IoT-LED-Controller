import Header from '../../components/Header';
import { useNavigate } from 'react-router-dom';

const LampGroup = ({ lampGroupId }) => {
    return (
        <div>
        <Header title="Lamp Groups" subtitle="List of lamp groups." />
        </div>
    );
    }

    export default LampGroup;