const API_URL = 'https://mechmind-api.onrender.com'; // Change to your API URL

export const login = async (username, password) => {
  // ... (same as the login function provided earlier)
};

export const logout = async () => {
  // Implement logout logic, if any API call is required
};

export const refreshToken = async () => {
  // Implement refresh token logic if applicable
};
